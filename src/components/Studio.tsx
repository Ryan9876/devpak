'use client';
import { PointerEvent, useMemo, useRef, useState } from 'react';
import type { PlanningProposal, RoomModel, RoomObject, WorkspaceMode } from '@/lib/room-model/types';
import { deterministicProposal } from '@/lib/planning/deterministic';
import { snapPoint, validatePlacement } from '@/lib/room-model/geometry';
import { buildVerificationGate } from '@/lib/room-model/verification';
import { createClient } from '@/lib/supabase/client';
import CapturePanel from './CapturePanel';
import MeasurementPanel from './MeasurementPanel';

const copy:Record<WorkspaceMode,string>={organize:'Optimize usable space and circulation without changing the physical room.',arrange:'Move and compare objects freely while keeping room dimensions and fixed constraints intact.',build:'Design against verified dimensions. Build-ready status stays locked until required evidence is verified.'};

export default function Studio({initialRoom,ownerId,demo=false}:{initialRoom:RoomModel;ownerId:string;demo?:boolean}){
 const [room,setRoom]=useState(initialRoom),[mode,setMode]=useState<WorkspaceMode>('organize'),[selected,setSelected]=useState(room.objects.find(x=>!x.fixed)?.id??room.objects[0]?.id??''),[proposal,setProposal]=useState<PlanningProposal|null>(null),[goal,setGoal]=useState('Improve storage and keep a clear path through the room.'),[status,setStatus]=useState('');const canvas=useRef<HTMLDivElement>(null);
 const selectedObject=room.objects.find(x=>x.id===selected);const gate=useMemo(()=>buildVerificationGate(room,['wall width','wall depth']),[room]);
 async function refresh(){if(demo)return;const supabase=createClient();const {data:r,error}=await supabase.from('rooms').select('*').eq('id',room.id).single();if(error||!r)return;const [m,o,op]=await Promise.all([supabase.from('room_measurements').select('*').eq('room_id',room.id),supabase.from('room_objects').select('*').eq('room_id',room.id),supabase.from('room_openings').select('*').eq('room_id',room.id)]);if(m.error||o.error||op.error)return;setRoom(prev=>({...prev,boundary:{widthUm:Number(r.width_um),depthUm:Number(r.depth_um),ceilingHeightUm:r.ceiling_height_um==null?null:Number(r.ceiling_height_um)},measurements:(m.data??[]).map((x:any)=>({id:x.id,label:x.label,valueUm:Number(x.value_um),toleranceUm:Number(x.tolerance_um),confidence:Number(x.confidence),source:x.source,verification:x.verification,deviceContext:x.device_context,calibration:x.calibration,correctionHistory:x.correction_history??[]})),objects:(o.data??[]).map((x:any)=>({id:x.id,label:x.label,kind:x.kind,position:{xUm:Number(x.x_um),yUm:Number(x.y_um)},size:{widthUm:Number(x.width_um),depthUm:Number(x.depth_um)},rotationDeg:Number(x.rotation_deg),fixed:x.fixed,clearanceUm:Number(x.clearance_um),source:x.source,confidence:x.confidence==null?null:Number(x.confidence),notes:x.notes})),openings:(op.data??[]).map((x:any)=>({id:x.id,wall:x.wall,offsetUm:Number(x.offset_um),widthUm:Number(x.width_um),kind:x.kind,swing:x.swing})),updatedAt:r.updated_at}));}
 async function persist(obj:RoomObject){if(demo){setRoom(x=>({...x,objects:x.objects.map(o=>o.id===obj.id?obj:o)}));return;}const supabase=createClient();const {error}=await supabase.from('room_objects').update({x_um:obj.position.xUm,y_um:obj.position.yUm,rotation_deg:obj.rotationDeg,width_um:obj.size.widthUm,depth_um:obj.size.depthUm,updated_at:new Date().toISOString()}).eq('id',obj.id).eq('owner_id',ownerId);if(error)setStatus(error.message);else setRoom(x=>({...x,objects:x.objects.map(o=>o.id===obj.id?obj:o)}));}
 function drag(e:PointerEvent<HTMLDivElement>,obj:RoomObject){if(obj.fixed||!canvas.current)return;e.currentTarget.setPointerCapture(e.pointerId);const rect=canvas.current.getBoundingClientRect();const move=(ev:globalThis.PointerEvent)=>{const x=(ev.clientX-rect.left)/rect.width*room.boundary.widthUm-obj.size.widthUm/2,y=(ev.clientY-rect.top)/rect.height*room.boundary.depthUm-obj.size.depthUm/2,next={...obj,position:snapPoint({xUm:Math.round(x),yUm:Math.round(y)})};const conflicts=validatePlacement(room,next);setStatus(conflicts[0]??'Placement is clear.');if(!conflicts.length)setRoom(r=>({...r,objects:r.objects.map(o=>o.id===obj.id?next:o)}));};const up=async()=>{window.removeEventListener('pointermove',move);window.removeEventListener('pointerup',up);const current=room.objects.find(o=>o.id===obj.id);if(current)await persist(current);};window.addEventListener('pointermove',move);window.addEventListener('pointerup',up,{once:true});}
 async function propose(){setStatus('Generating a geometry-checked proposal…');if(demo){setProposal(deterministicProposal(room,mode));setStatus('Local deterministic proposal ready.');return;}try{const res=await fetch('/api/ai/plan',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({mode,goal})}),json=await res.json();if(!res.ok)throw new Error(json.error||'Proposal failed');setProposal(json.proposal);setStatus('Proposal ready. Geometry conflicts are shown separately.');}catch(err){setProposal(deterministicProposal(room,mode));setStatus(`${err instanceof Error?err.message:'AI unavailable'} Deterministic fallback shown.`);}}
 async function accept(){if(!proposal)return;const changed=room.objects.map(o=>{const p=proposal.placements.find(x=>x.objectId===o.id);return p?{...o,position:p.position,rotationDeg:p.rotationDeg}:o});const conflicts=changed.flatMap(o=>validatePlacement({...room,objects:changed},o));if(conflicts.length){setStatus(conflicts[0]);return;}for(const o of changed)if(o.position.xUm!==room.objects.find(x=>x.id===o.id)?.position.xUm||o.position.yUm!==room.objects.find(x=>x.id===o.id)?.position.yUm)await persist(o);setProposal(null);setStatus('Proposal applied to the Room Model.');}
 const scaleX=(v:number)=>`${v/room.boundary.widthUm*100}%`,scaleY=(v:number)=>`${v/room.boundary.depthUm*100}%`;
 return <main className="studio-page"><div className="studio-top"><div className="studio-title"><h1>{room.name} Studio</h1><p>One Room Model · {room.objects.length} objects · {room.measurements.length} measurements {demo&&<span className="demo-pill">demo data</span>}</p></div><div className="mode-switch" aria-label="Workspace mode">{(['organize','arrange','build'] as WorkspaceMode[]).map(x=><button key={x} className={mode===x?'active':''} onClick={()=>{setMode(x);setProposal(null)}}>{x[0].toUpperCase()+x.slice(1)}</button>)}</div></div><div className="studio-grid"><aside className="panel tool-panel"><div className="mode-copy">{copy[mode]}</div><h2>Room objects</h2><div className="tool-list">{room.objects.map(o=><button key={o.id} className={selected===o.id?'active':''} onClick={()=>setSelected(o.id)}>{o.label}{o.fixed?' · fixed':''}</button>)}</div><CapturePanel ownerId={ownerId} roomId={room.id} onSaved={refresh}/><MeasurementPanel ownerId={ownerId} roomId={room.id} measurements={room.measurements} onChanged={refresh}/></aside><section className="panel canvas-panel"><div className="canvas-toolbar"><span>{Math.round(room.boundary.widthUm/1000)} × {Math.round(room.boundary.depthUm/1000)} mm</span><span>50 mm snap · fixed constraints protected</span></div><div className="room-canvas" ref={canvas}>{room.openings.map(o=><div key={o.id} className="opening-marker" title={`${o.kind} opening`} style={o.wall==='south'||o.wall==='north'?{left:scaleX(o.offsetUm),width:scaleX(o.widthUm),height:6,[o.wall==='north'?'top':'bottom']:0}:{top:scaleY(o.offsetUm),height:scaleY(o.widthUm),width:6,[o.wall==='west'?'left':'right']:0}}/>)}{room.objects.map(o=><div key={o.id} className={`canvas-object${o.fixed?' fixed':''}${selected===o.id?' selected':''}`} onPointerDown={e=>drag(e,o)} onClick={()=>setSelected(o.id)} style={{left:scaleX(o.position.xUm),top:scaleY(o.position.yUm),width:scaleX(o.size.widthUm),height:scaleY(o.size.depthUm),transform:`rotate(${o.rotationDeg}deg)`}}>{o.label}</div>)}</div></section><aside className="panel inspector"><h2>{mode[0].toUpperCase()+mode.slice(1)}</h2>{selectedObject&&<><p><b>{selectedObject.label}</b>{selectedObject.fixed?' · fixed':''}</p><div className="status-card">Position {Math.round(selectedObject.position.xUm/1000)}, {Math.round(selectedObject.position.yUm/1000)} mm<br/>Size {Math.round(selectedObject.size.widthUm/1000)} × {Math.round(selectedObject.size.depthUm/1000)} mm</div></>}{mode==='build'&&<div className={`status-card ${gate.allowed?'':'warning'}`}><b>{gate.allowed?'Build evidence ready':'Build evidence locked'}</b><br/>{gate.allowed?'Required measurements are verified.':`Missing: ${[...gate.missing,...gate.unverified].join(', ')||'verification'}`}</div>}<label>Goal<input value={goal} onChange={e=>setGoal(e.target.value)}/></label><button className="button primary" onClick={propose}>Generate {mode} proposal</button>{proposal&&<div className="proposal-card"><h3>{proposal.title}</h3><p>{proposal.summary}</p><ul>{proposal.rationale.map(x=><li key={x}>{x}</li>)}</ul>{proposal.conflicts.length>0&&<p><b>Conflicts:</b> {proposal.conflicts.join(' ')}</p>}<button className="small-button dangerless" onClick={accept} disabled={proposal.conflicts.length>0}>Apply proposal</button></div>}{status&&<div className="status-card">{status}</div>}{!demo&&<form action="/auth/signout" method="post"><button className="small-button" type="submit">Sign out</button></form>}</aside></div></main>;
}
